import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  button: {
    position: 'absolute',
    top: 0,
    left: 0,
    width: '100%',
    height: '100%',
    zIndex: 2,
  },
  label: {
    marginBottom: 10,
    fontSize: 16,
    fontWeight: '500',
  },
});

export default styles;
