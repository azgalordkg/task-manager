import { StyleSheet } from 'react-native';

const styles = () =>
  StyleSheet.create({
    mainWrapper: {
      marginTop: 20,
      width: '100%',
      flex: 1,
      alignItems: 'flex-start',
    },
    fieldsWrapper: {
      flex: 1,
      width: '100%',
    },
  });

export default styles;
