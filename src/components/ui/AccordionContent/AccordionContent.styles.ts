import { StyleSheet } from 'react-native';

import { COLORS } from '@/constants';

const styles = StyleSheet.create({
  contentContainer: { backgroundColor: COLORS.GREY_BG },
});

export default styles;
