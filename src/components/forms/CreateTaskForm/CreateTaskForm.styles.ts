import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  inputsWrapper: {
    rowGap: 6,
    marginBottom: 40,
  },
  dateWrapper: {
    rowGap: 12,
  },
});

export default styles;
