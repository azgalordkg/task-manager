import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  dateButtonsWrapper: {
    flexDirection: 'row',
    flexWrap: 'wrap',
    width: '100%',
  },
  dateButtonContainer: {
    marginRight: 6,
  },
});

export default styles;
