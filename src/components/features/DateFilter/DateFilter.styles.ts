import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  dateButtonsWrapper: {
    flexDirection: 'row',
    width: '100%',
  },
  dateButtonContainer: {
    marginRight: 6,
  },
});

export default styles;
