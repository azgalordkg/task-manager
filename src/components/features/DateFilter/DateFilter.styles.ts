import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  dateButtonsWrapper: {
    flexDirection: 'row',
    flexWrap: 'wrap',
    width: '100%',
    columnGap: 6,
    rowGap: 8,
  },
});

export default styles;
